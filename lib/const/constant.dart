import 'package:flutter/material.dart';

const cardBackgroundColor = Color(0xFF40394A);
const primaryColor = Color(0xFF52057B);
const secondaryColor = Color(0xFF892CDC);
const backgroundColor = Color(0xFF1C1427);
const selectionColor = Color(0xFFBC6FF1);

const defaultPadding = 20.0;
const maxDesktopWidth = 800.0;
const screenPadding = 32.0;

const startPlayerClientEndpoint = 'play';
const playerClientBaseUrl = 'http://localhost:8080';

const currencySymbol = ' EUR';
